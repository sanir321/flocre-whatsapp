[build]
builder = "NIXPACKS"
buildCommand = "cd evolution-api && npm ci && npm run build && cd ../flowcore-ai && npm ci && npm run build"

[deploy]
startCommand = "node start.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30

[env]
NODE_ENV = "production"
